# current-state.md

**This file describes what actually works right now — not what's planned, not what's in progress. If it's not implemented and tested, it's not listed here as working.** Read this before `TASKS.md` to know where the project actually stands.

Last updated: 2026-08-06

---

## Overall Status

`v1 — Phase 3 (Test Harness UI)`
*(update this line as phases in TASKS.md complete: Phase 0 → Phase 1 → Phase 2 → Phase 3 → Phase 4 → Phase 5)*

---

## Component Status

| Component | Status | Notes |
|---|---|---|
| `core/face_detector.py` | Implemented & validated (single + multiple faces; angled = known miss) | `detect_faces(frame) -> list[(x,y,w,h)]` via MediaPipe `solutions.face_detection`. `tests/test_face_detector.py` passes on MediaPipe 0.10.21 (single + multi + guards + angled characterization). Angled/partial faces may be missed — accepted v1 limitation, see ISSUE-002 |
| `core/ocr_detector.py` | Implemented & validated (synthetic + real screenshot) | `detect_text(frame) -> list[(text, (x,y,w,h))]` via **RapidOCR on ONNX Runtime** (D17, supersedes the planned Tesseract/D6). Backend isolated in-module; callers see only normalized `(text, bbox)`. `tests/test_ocr_detector.py` — `17 passed`: synthetic UI/email/phone/IPv4/card/small-text + guards, **plus a real-screenshot integration test** (`tests/assets/ocr_real_screen.png`) that reads all 5 planted values cleanly. Not yet wired into the video pipeline. |
| `core/pii_matcher.py` | Implemented & tested | Four regex functions: `is_email()`, `is_phone()`, `is_card()`, `is_ipv4()` (D7), plus `find_pii(text) -> list[(pii_type, start, end, value)]` which reports **where** each PII value sits (regex match span) so a caller can redact only the value and leave a surrounding label ("Email:", "CARD", ...) visible; overlaps are resolved by a documented precedence (EMAIL, IP, CARD, PHONE — the pipeline's prior classification order), so a value-only box classifies identically. Patterns are **configurable** (D23) via `get_patterns()`/`get_default_patterns()`/`set_patterns()`/`reset_patterns()` over an active-patterns layer the `is_*()`/`find_pii()` functions consult; `set_patterns()` compiles **all-or-nothing** (bad regex / unknown type → `ValueError`, active patterns untouched). Defaults are the shipped patterns and unchanged until overridden (test-harness Settings screen only). `tests/test_pii_matcher.py` — `35 passed` covering valid/invalid samples + edge cases + the get/set/reset/all-or-nothing API + `find_pii` spans (label excluded, value-only whole-string, variable label width, separator variants, precedence, multiple values). |
| `core/redactor.py` | Implemented & tested (rendering only) | `blur_region()`, `box_region()`, `fake_data_region()` draw a redaction treatment onto a supplied bbox, plus (fake-data quality, 2026-08-06) `estimate_text_style()` and `inpaint_region()`. Rendering-only — caller supplies bbox + (for fake-data) the replacement string + an optional source-style dict; no detection/matching/fake-data generation here. `estimate_text_style(frame, bbox)` reads the source region **before** removal (k-means k=2 → minority cluster = text color, majority = bg color; Otsu + minority-polarity contour trace → text height; baseline/width) and returns a style dict, or `None` for an invalid/empty box. `inpaint_region(frame, mask, method)` reconstructs masked pixels via OpenCV `cv2.inpaint`, `"telea"` or `"ns"` (Navier-Stokes), radius 3 — **both remain available, no winner chosen**. `fake_data_region()` gained optional `inpaint_method` + `style`: it removes the original by inpainting (mask = bbox + small margin) or solid fill (default/back-compat), then draws the replacement in the style's fg color on the reconstructed bg, sized to the estimated glyph height (Hershey cap ≈ height/22, then width-fitted). Colour/height are **heuristic** and the Hershey font ≠ source font — no seamless/font-exact claim (D24). `tests/test_redactor.py` — `14 passed`; fake-data-quality behavior covered by `tests/test_fake_data_quality.py` — `26 passed`. `fake_data_region()` is wired into the pipeline; the other treatments are used by `zone_manager`/pipeline. |
| `core/zone_manager.py` | Implemented & tested | `ZoneManager` stores `(x,y,w,h)` pixel-coord zones (add/remove/get/clear) and `apply_zones(frame, mode)` redacts them every frame by reusing `redactor` (blur/box). `tests/test_zone_manager.py` — `19 passed`. Not yet wired into the pipeline. |
| `core/video_pipeline.py` | Implemented & tested (orchestration + OCR sampling + audio mux + preview hook) | `process_video(input_path, output_path, mode="blur"\|"fake_data", zones=None, ocr_sample_rate=1, face_min_confidence=None, inpaint_method="telea", progress_callback=None, preview_callback=None) -> summary dict`. Ties together face_detector, ocr_detector, pii_matcher, redactor, ZoneManager, fake_data via their existing public APIs (no duplicated logic). Per frame: detect faces → (OCR + classify **only on every Nth frame**, persisted between samples) → **faces always blurred** → PII redacted per mode (blur, or fake_data). In fake_data mode the pipeline calls `redactor.estimate_text_style()` on each PII value's bbox **before** the pixels are destroyed, generates the replacement via `fake_data.generate(pii_type, original=value)`, and persists `(pii_type, value_bbox, replacement, style)` as a 4-tuple; the redaction pass then calls `fake_data_region(frame, bbox, replacement, inpaint_method=inpaint_method, style=style)` so the value is inpainted (TELEA/NS, `inpaint_method` param, default `"telea"`) and re-drawn in the source's estimated colour/size. Both inpainting methods remain selectable (D24 — no winner picked). Then static zones are applied and the frame is written. When one OCR box holds a label + value ("Email: a@b.com"), only the value's sub-region is redacted: `pii_matcher.find_pii()` gives the value's character span and `_span_to_bbox()` maps it to a tighter horizontal region of the OCR box (proportional per-character approximation — RapidOCR gives per-line, not per-glyph, boxes; ISSUE-006), so the label stays visible in both modes; value-only boxes are unchanged (integer-exact full bbox), multiple values per box each get a region, and the tightened bbox is what persists between samples / fake-data covers. Frames go to a temp video-only intermediate (OpenCV, mp4v), then **ffmpeg (imageio-ffmpeg) transcodes the video to browser-playable H.264** (`-c:v libx264 -pix_fmt yuv420p -movflags +faststart`) and re-muxes the original audio (stream-copied, not re-encoded) into the final output; intermediate always cleaned up (D8). H.264/yuv420p so the output plays in HTML5 `<video>` (Chrome/Edge/Firefox) as well as VLC/QuickTime — mp4v is not browser-decodable. Source without audio → valid video-only output. `ocr_sample_rate` functional; faces + zones every frame; fake-data value persisted per sample (D18). Optional `face_min_confidence` (D23) is validated to `[0.0, 1.0]`; `None` (default) leaves the detector's own default in force (unchanged behavior), else it is forwarded to `face_detector.detect_faces(min_confidence=…)`. Optional `preview_callback` (D21) is a **pure observability hook** for the web harness — fired **after** the frame is written with `{frame, frame_number, total, faces, pii, zones}` (pii = detections actually applied, fresh or persisted); no-op by default, changes no redaction/summary behavior, cannot contaminate output. Preserves dims/FPS; releases OpenCV resources in `finally`. `tests/test_video_pipeline.py` — `50 passed`. ffmpeg mux failure → `RuntimeError`. Non-mp4-copyable audio codec limitation: ISSUE-004. Between-samples moving-text miss: ISSUE-003. |
| `utils/fake_data.py` | Implemented & tested | `generate(pii_type, original=None)` returns a clearly-synthetic placeholder for "EMAIL"/"PHONE"/"CARD"/"IP" (matches pii_matcher's types; name not supported per D7). The optional `original` lets phone/card **mirror the source formatting** — separator (parens/hyphen/space/digits-only) and card digit-grouping — so the replacement keeps the same shape; email/IP accept it for API parity but ignore it. stdlib `random` only. Reserved/documentation ranges so values are never real. Wired into `video_pipeline` fake_data mode. `tests/test_fake_data.py` — `11 passed`; formatting preservation also covered by `tests/test_fake_data_quality.py`. |
| `tools/webtest/server.py` + `upload_support.py` + `job.py` + `settings_store.py` | 4 screens; **all four functional** (Upload + Processing + Results + Settings; real pipeline wired) + **fake_data dual-output comparison** (D25) | Flask serves 4 screens via `templates/base.html` (top nav, single-column per design.md §3): `/`→Upload, `/processing`, `/results`, `/settings`. **Upload screen is live** (design.md §2.1): file-picker **and** drag/drop upload with two-stage validation (extension allowlist → real OpenCV decode of frame 0), first-frame extraction shown on a `<canvas>`, click-drag rectangular static zones with per-zone delete, Blur/fake_data mode toggle. Zones converted **display→original-video-pixel coords server-side** in `/zone` (D19), stored as `[x,y,w,h]` consumable by `ZoneManager.add_zone()`. **Processing screen is live** (design.md §2.2, D20): **Process Video** POSTs `/process`, which runs `core.video_pipeline.process_video()` (its public API only — no duplicated pipeline logic) in a background daemon thread (`job.py`) and returns **HTTP 202** without blocking; the browser navigates to `/processing?job=<uid>` and polls `/job/<uid>` (~500 ms) to drive a live progress bar, a per-stage status table, and a scrolling technical log. Stage/log numbers derive **only** from the pipeline's `progress_callback` + returned summary (no fabricated internals). One job at a time (duplicate active start → **HTTP 409**); source (`uploads/`) and output (`outputs/`) kept in separate dirs. Pipeline exceptions → controlled error state; summary stored for the later Results task. Pure validation + coord math in `upload_support.py` (no Flask import). **Live current-frame preview is now functional** (design.md §2.2, D21): the pipeline's new optional `preview_callback` (fired after each frame is written) feeds `job.py`, which builds a throttled, downscaled JPEG with labeled face/PII/zone boxes overlaid (retains only the latest image), served via `GET /job/<uid>/preview` (`image/jpeg`, `no-store`, 404 until first frame) — **never base64'd into the `/job` poll**; `app.js` refreshes the `<img>` only when `preview_seq` changes. Overlay is diagnostic-only (drawn post-write, cannot alter the redacted output). **Results screen is now functional** (design.md §3, D22): the completion handler surfaces a **View results** link to `/results?job=<uid>`; `/results` reads only the completed `ProcessingJob`/`_STATE` for that upload and renders **before/after** `<video>` previews served through **controlled id-keyed routes** — `GET /video/<uid>/original` and `GET /video/<uid>/processed` (paths resolved from server-owned `_STATE` or `job`, never a client-supplied path; range-request streaming via `send_file(conditional=True)`), a **summary panel** built **only** from the pipeline's returned summary (`frames_processed`, `faces_blurred`, `pii_by_type` split emails/phones/cards/IPs + total, `zones_applied` — nothing fabricated), and a **Download** button via `GET /download/<uid>` (`as_attachment=True`, real basename preserved). All non-happy states handled: unknown/absent job → error page; still-running or errored job → message + link back to Processing; completed-but-output-missing → error. Arbitrary/traversal ids are rejected (404, never filesystem access) — verified by `tests/test_webtest_results.py` (`14 passed`). The Results **"Flag an issue"** in-app log was intentionally **dropped** (D22) — manual misses go directly in `docs/known-issues.md`. **Settings screen is now functional** (design.md §2.4, D23): tuning knobs that actually change what a **newly started** job does. `settings_store.py` holds process-local settings (threading.Lock-guarded module dict — no DB/accounts/Redis/config-service/dependency, D10) and validates them all-or-nothing: OCR sample rate (positive int), face confidence (`[0,1]`), and the four PII regex patterns (pushed into `core.pii_matcher`; a bad regex is rejected and the last valid config kept). Routes: `GET /settings` (renders current + default values), `GET /settings/values` (JSON), `POST /settings` (validate+apply, **400** on invalid without corrupting current), `POST /settings/reset` (restore defaults incl. `pii_matcher.reset_patterns()`). `/process` reads `settings_store.processing_kwargs()` at start, so a job forwards the saved OCR rate + face confidence into `process_video()` (PII patterns already apply globally via `pii_matcher`); with nothing saved, a job uses the shipped defaults (rate 1, detector-default confidence) — unchanged behavior. UI (`settings.html` + `app.js` + `style.css`) gives an OCR number input, a confidence slider, four regex inputs, Save/Reset, and inline success/error feedback in the existing design language (no redesign). Verified by `tests/test_webtest_settings.py` (`11 passed`) + core `tests/test_pii_matcher.py`/`tests/test_video_pipeline.py` additions; tests restore process-global settings so nothing leaks across files. No new deps. **Polish pass (2026-08-05):** Results `<video>` previews now play in-browser — the pipeline's final MP4 is H.264 (was mp4v, which HTML5 `<video>` can't decode); the completion screen shows **one** job-aware **View results** action (the duplicate static button was removed); and the top-nav Processing/Results tabs carry the current job id (server-injected `active_job_uid` via `_current_job_uid()` + `@app.context_processor`, preferring the page's `?job=` param then `_ACTIVE_JOB_UID`), so navigating away mid-job and back reconnects to the SAME background job instead of an idle/Unknown-job screen — a GET never starts/stops/replaces a job and `process_video()` still runs exactly once per job. The **original** preview is the uploaded file served as-is, so it renders only for browser-supported source codecs (mp4v sample fixtures show controls-but-blank in-browser though they download/play externally — ISSUE-005). Verified by `tests/test_webtest_navigation.py` (`12 passed`). **Fake-data dual-output comparison (2026-08-06, D25):** In fake_data mode, a harness job now processes the input **twice** — once with `inpaint_method="telea"`, once with `"ns"` — writing method-specific outputs (`<stem>_telea.mp4`, `<stem>_ns.mp4`) so both classical-inpainting fills can be manually compared. Blur mode unchanged (still runs once). `ProcessingJob` presents the two runs as ONE job: overall progress spans both passes (0–50%, 50–100%), COMPLETE only after both succeed, frame/PII counters reset per pass (no double-count), and either-method failure → controlled error with `failed_method` set. Results shows **Original + TELEA + NS** (3-up) for fake_data, with per-method video/download routes + a stdlib-`zipfile` "Download Both (ZIP)" containing exactly the two processed outputs; blur keeps Original + Redacted (2-up) with single download. Summary shown once (inpainting changes pixels, not detections); if the two passes' detection counts differ, `summary_discrepancy` is flagged on Results + logged. All routes enforce controlled id-keyed path resolution (D22 security model); unknown id / incomplete job / missing output / wrong mode → 404/409. Validated by `tests/test_webtest_dual_output.py` (`29 passed`). Bug fix (2026-08-06): routes now use `job.mode` and fall back to `job.output_paths` / `job.output_path` so Results and video/download routes remain functional even if `_STATE` UI mode is toggled post-completion. **Processing page UI & logging update (2026-08-06):** Redesigned `processing.html` into a full-width, no-scroll 2-column dashboard grid (Progress card top; Pipeline stages + Current frame preview left; Technical log full height right), removed redundant header text, and updated `job.py` frame log cadence to log progress at least once per second (`>= 1.0s` or frame 1 / total) instead of waiting 30 frames. Full webtest suites → `85 passed`. No winner chosen between TELEA and NS — this is a temporary manual comparison capability (D25).
| `gui/app.py` | Not started | Blocked on core pipeline validation (see TASKS.md Phase 2 gate) |
| `tests/sample_videos/*.mp4` | Phase 2 fixtures generated + acceptance-validated | 5 deterministic fixtures built by `tests/make_sample_videos.py`. End-to-end validated 2026-08-04 via `tests/phase2_validate.py` (real `process_video`, no mocks): static faces/PII/zone pass ~100%; moving-ticker miss quantified (ISSUE-003). Git-ignored (regenerate, don't commit). |
| Packaging (PyInstaller) | Not started | Blocked on GUI |

---

## What's Been Validated (per TESTING.md)

- TESTING.md 3.1 — face detector, single frontal face: **PASS** (`tests/test_face_detector.py`, MediaPipe 0.10.21, 2026-08-04).
- TESTING.md 3.1 — face detector, multiple faces: **PASS** (`tests/test_face_detector.py`, `9 passed`, MediaPipe 0.10.21, 2026-08-04) — 3 faces detected in `tests/assets/multiple_faces.jpg`.
- TESTING.md 3.1 — face detector, angled/partial face: **CHARACTERIZED** — angled face in `tests/assets/angled_face.jpg` was **missed (0 detected)**, the documented, accepted v1 limitation (ISSUE-002).
- TESTING.md 3.2 — OCR detector, static text (email/phone/card/IP + UI text): **PASS** (`tests/test_ocr_detector.py`, `17 passed`, RapidOCR 3.9.2 / ONNX Runtime 1.28.0, 2026-08-04). OCR only — PII regex matching is pii_matcher.py's job, tested separately.
- TESTING.md 3 — output audio track preserved: **PASS (unit level)** (`tests/test_video_pipeline.py::TestProcessVideoAudioMux`, 2026-08-04). ffmpeg (imageio-ffmpeg) re-muxes the source audio into the final output (verified an audio stream is present); a source with no audio yields a valid video-only output; ffmpeg failure surfaces as `RuntimeError` and the temp intermediate is always cleaned up. Full end-to-end audio-in-sync confirmation on real recordings is Phase 2.
- **TESTING.md 3.1–3.5 — full pipeline end-to-end (Phase 2 acceptance): PASS for all static content; moving-text limitation quantified.** `tests/phase2_validate.py` ran the real `process_video()` (no mocks) on all 5 fixtures 2026-08-04:
  - Faces — basic 1/1, multi 3/3 blurred every frame; face-region sharpness −94% (Laplacian var in→out). MediaPipe still localizes the *blurred blob* in output (60/60) — the region is blurred, not unredacted (transparency note, not a bug).
  - PII (static, `test_pii_text.mp4`) — all 4 planted values readable in **0/60** output frames in **both** blur and fake_data modes; no false positives on ordinary UI text.
  - Zones — interior variance 633→0.7 (fully blurred), adjacent strip 219→221 (untouched), applied 60/60 frames.
  - Integrity — 960×540 / 10 fps / 60 f / 6.0 s preserved on every output; audio present on the two audio fixtures, absent on the rest.
  - Moving PII (`test_mixed.mp4`) — static PII fully redacted; scrolling ticker only partially covered (CARD 27/60 @ rate 1, IP 3/60), the accepted ISSUE-003 tradeoff (not tuned around). Manual VLC/WMP A/V-sync spot-check remains a human step.

---

## Known Limitations Currently Accepted

*(Mirror the high-level summary here; full detail lives in known-issues.md. Keep this list short — just enough to orient a new session.)*

- MediaPipe is pinned to `mediapipe==0.10.21` in `requirements.txt` because 1.0.0 (and 0.10.35 here) removed the legacy `solutions.face_detection` API that `core/face_detector.py` uses per D5. See ISSUE-001 (resolved).
- Angled/partial faces may be missed by the face detector (observed: 0 detected on `tests/assets/angled_face.jpg`). Accepted v1 limitation per TESTING.md 3.1 — see ISSUE-002.
- With OCR frame-sampling (`ocr_sample_rate > 1`), PII that moves/scrolls or appears-and-vanishes between samples can be missed or lag its box — accepted v1 tradeoff per TESTING.md 3.2, see ISSUE-003. Lower the sample rate (→ `1`) to trade speed for coverage; faces and static zones are unaffected (every frame).
- The Results **Original** preview serves the uploaded file as-is, so it renders in-browser only for browser-supported source codecs; mp4v sample fixtures (`tests/sample_videos/*.mp4`) show controls-but-blank in HTML5 `<video>` though they download/play externally. The **processed** output is always browser-playable H.264. Accepted v1 limitation — see ISSUE-005.
- When an OCR box mixes a label and a PII value, only the value is redacted, but the value's bbox is a **proportional per-line approximation** (RapidOCR gives per-line, not per-glyph, boxes) — with variable-width fonts the redaction edge can be a few pixels off, though it always stays inside the OCR box and always fully covers the value. Accepted v1 limitation — see ISSUE-006.
- Fake-data replacement approximates but does not reproduce the source: text colour and glyph height are **heuristic estimates** (k-means / contour trace) and the replacement is always drawn in `FONT_HERSHEY_SIMPLEX`, not the original font, so it is **not** seamless or font-exact (by design — no GAN/diffusion, D24). Classical inpainting (TELEA/NS) can smear on textured/non-flat backgrounds. At `ocr_sample_rate=1` OCR reruns every frame, so the randomized fake value regenerates per frame and visibly flickers; persistence (D18) only stabilizes it *between* samples when the rate is `>1`. Accepted v1 limitation — see ISSUE-007.

---

## How to Update This File

- After implementing/testing any component, update its row in the table above.
- Keep descriptions to one line — detail belongs in `development-log.md` (what changed) or `known-issues.md` (what's broken), not here.
- This file should always be readable in under a minute and give an accurate snapshot — if it takes longer than that to read, it's gotten too detailed and needs trimming.